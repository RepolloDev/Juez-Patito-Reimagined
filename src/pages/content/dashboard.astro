---
import Layout from '@layouts/Layout.astro'
import { isLoggedIn } from '@utils/auth/login'

const isUserLogged = isLoggedIn(Astro.cookies)
if (!isUserLogged) {
  return Astro.redirect('/login')
}
---

<Layout title="Juez Patito - Login">
  <main>
    <h2>Dashboard</h2>
  </main>
</Layout>
